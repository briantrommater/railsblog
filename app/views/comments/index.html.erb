<main id='comments-index'>
  <% @comments.each do |comment| %>
      
      <div class='commentBox'>
      
        Comment: <%= comment.body %><br/>
      </div>
  <% end %>



  <p>
    <%= link_to('Create Comment', new_comment_path)%>
  </p>

  <% if session[:user_id] %>
     hi you are logged in!
    <%= link_to 'Signout', destroy_session_path%>
  <% else %>
    <%= link_to 'Signin', new_session_path %>
  <% end %>

</main>

<% if false %>

<%= link_to(comment.user.name, comment) %> <br/>

<% end %>