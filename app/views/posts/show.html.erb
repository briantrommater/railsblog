
<%= @post.body %>

<%= link_to("Edit Post", edit_post_path(@post)) %><br>
<%= link_to("Delete Post", @post, method: 'delete') %><br>
<%= link_to("Add Comment", new_post_comment_path(post_id: @post.id)) %>

<h3><% @post.comments.reverse.each do |f| %> </h3> 
            <div id="single-comment">
                <h4>Comment</h4>
                <h5>From - <%= f.user.name %></h5>

                <h5><%= f.body %> </h5>

                <br>

                
                
                <a href ="/posts/<%=@post.id%>/comments/<%=f.id%>/edit">edit comment</a>
                <br>
               
                
            </div>
        <% end %>

<%=image_tag "dog1.jpeg" %>